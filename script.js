console.log(1);
//  console is een object, 
// log is een function (method) met een argument
// de dot notatie geeft aan dat de log function bij het console object hoort
function clown(name){

    // return [name, shoeSize]
    return {
        name, shoeSize
    }
}


const car = function ( brand, color, topSpeed){

return{
    brand, color, topSpeed,
}

}
const building = (floors, roofType, elevator) => {

}

console.log(clown('bomba', 80))